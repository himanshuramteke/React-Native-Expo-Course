import React from "react";
import { StyleSheet, Text, View } from "react-native";

const CustomNotFoundForProfile = () => {
  return (
    <View>
      <Text>CustomNotFoundForProfile</Text>
    </View>
  );
};

export default CustomNotFoundForProfile;

const styles = StyleSheet.create({});
